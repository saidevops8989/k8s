#####k8scmd to create pod #####

kubectl run mypod --restart=Never --image=nginx

######k8s to create deployment ######

kubectl create deployment mydeployment --image=nginx -n namespace

kubectl get deployment -n namespace

kubectl get pods -n namespace

kubectl expose deployment mydeployment --type=NodePort --port=80 -n cse

and then access from browser fot the nodeport


kubectl delete deployment/mydeployment

######service#######

kubectl 
kubectl describe nodes | grep -A2 -e "^\\s*CPU Requests" -e "^\\s*CPU Limits" -e "^\\s*Memory Requests" -e "^\\s*Memory Limits"
